import { by, element, ElementFinder } from "protractor";

export class ClaimsHistoryPageObject {
    // tslint:disable-next-line: no-empty
    public claimshistory: ElementFinder;
    constructor() {
  //  this.claimshistory = element(by.xpath("//span[@class='g-btn-text' and text()='Next']"));
}}
