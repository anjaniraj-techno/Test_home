import { by, element, ElementFinder } from "protractor";

export class UnderwrittingPageObject {
    // tslint:disable-next-line: no-empty
    public underwriternext: ElementFinder;
    constructor() {
  //  this.underwriternext = element(by.xpath("//span[@class='g-btn-text' and text()='Next']"));
    } }
