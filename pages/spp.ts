import { by, element, ElementFinder } from "protractor";

export class SppObject {
    // tslint:disable-next-line: no-empty
    public sppnext: ElementFinder;
    constructor() {
  //  this.sppnext = element(by.xpath("//span[@class='g-btn-text' and text()='Next']"));
    } }
