import { by, element, ElementFinder } from "protractor";

export class PricingPageObject {
    // tslint:disable-next-line: no-empty
    public pricingnext: ElementFinder;
    constructor() {
  //  this.pricingnext = element(by.xpath("//span[@class='g-btn-text' and text()='Next']"));
    } }
