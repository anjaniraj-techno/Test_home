import { by, element, ElementFinder } from "protractor";

export class WatercraftPageObject {
    // tslint:disable-next-line: no-empty
    public watercraftnext: ElementFinder;
    constructor() {
 //   this.watercraftnext = element(by.xpath("//span[@class='g-btn-text' and text()='Next']"));
    } }
